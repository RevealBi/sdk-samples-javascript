server.port=5111

# Optional path to a local RevealEnginePrg executable when developing against a local engine build.
# Leave empty to use the native engine bundled with the SDK dependency.
reveal.engine.path=${REVEAL_ENGINE_PATH:}
